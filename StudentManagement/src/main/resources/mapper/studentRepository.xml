<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="raisetech.StudentManagement.repository.StudentRepository">

  <!-- 受講生の全件検索 -->
  <select id="searchStudent" resultType="raisetech.StudentManagement.data.Student">
    SELECT *
    FROM student
  </select>

  <!-- 受講生の検索 -->
  <select id="searchStudentById" resultType="raisetech.StudentManagement.data.Student">
    SELECT *
    FROM student
    WHERE id = #{id}
  </select>

  <!-- 受講生の複数条件での検索 -->
  <select id="searchStudentByCondition" resultType="raisetech.StudentManagement.data.Student">
    SELECT *
    FROM student
    WHERE 1 = 1
    <if test="name != null and name != ''">
      AND name LIKE CONCAT('%', #{name}, '%')
    </if>
    <if test="kanaName != null and kanaName != ''">
      AND kana_name LIKE CONCAT('%', #{kanaName}, '%')
    </if>
    <if test="nickname != null and nickname != ''">
      AND nickname LIKE CONCAT('%', #{nickname}, '%')
    </if>
    <if test="email != null and email != ''">
      AND email LIKE CONCAT('%', #{email}, '%')
    </if>
    <if test="area != null and area != ''">
      AND area LIKE CONCAT('%', #{area}, '%')
    </if>
    <if test="sex != null and sex != ''">
      AND sex LIKE CONCAT('%', #{sex}, '%')
    </if>
    <if test="remark != null and remark != ''">
      AND remark LIKE CONCAT('%', #{remark}, '%')
    </if>
    <if test="age != null">
      AND age = #{age} <!-- 年齢は完全一致のまま -->
    </if>
    <if test="isDeleted != null">
      AND is_deleted = #{isDeleted}
    </if>
  </select>

  <!-- 受講生の登録 -->
  <insert id="registerStudent">
    INSERT INTO student (
    id, name, kana_name, nickname, email, area, age, sex, remark, is_deleted
    ) VALUES (
    #{id}, #{name}, #{kanaName}, #{nickname}, #{email}, #{area}, #{age}, #{sex}, #{remark}, FALSE
    )
  </insert>

  <!-- 受講生情報の更新 -->
  <update id="updateStudent">
    UPDATE student
    SET
    name= #{name}, kana_name = #{kanaName}, nickname = #{nickname},
    email = #{email}, area = #{area}, age = #{age}, sex = #{sex}, remark = #{remark},
    is_deleted = #{isDeleted} WHERE id = #{id}
  </update>


  <!-- 受講生コース情報の全件検索　-->
  <select id="searchStudentCourseList" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT *
    FROM student_course
  </select>

  <!-- 受講生コース情報の検索　-->
  <select id="searchStudentCourse" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT *
    FROM student_course
    WHERE id = #{id}
  </select>

  <!-- 受講生IDに紐づく受講生コース情報の検索　-->
  <select id="searchStudentCourseListByStudentId" resultType="raisetech.StudentManagement.data.StudentCourse">
    SELECT *
    FROM student_course
    WHERE student_id = #{studentId}
  </select>

  <!-- 受講生コース情報の登録 -->
  <insert id="registerStudentCourse">
    INSERT INTO student_course (
    id, student_id, course_master_id, course_start_at, course_end_at
    ) VALUES (
    #{id}, #{studentId}, #{courseMasterId}, #{courseStartAt}, #{courseEndAt}
    )
  </insert>

  <!-- 受講生コース情報の更新 -->
  <update id="updateStudentCourse">
    UPDATE student_course SET course_master_id = #{courseMasterId} WHERE id = #{id}
  </update>

  <!-- 申込状況の全件検索　-->
  <select id="searchCourseApplicationStatusList" resultType="raisetech.StudentManagement.data.CourseApplicationStatus">
    SELECT *
    FROM course_application_status
  </select>

  <!-- 受講生コースIDに紐づく申込状況の検索　-->
  <select id="searchCourseApplicationStatusByStudentCourseId" resultType="raisetech.StudentManagement.data.CourseApplicationStatus">
    SELECT *
    FROM course_application_status
    WHERE student_course_id = #{studentCourseId}
  </select>

  <!-- 申込状況の登録 -->
  <insert id="registerCourseApplicationStatus">
    INSERT INTO course_application_status (
    id, student_course_id, status
    ) VALUES (
    #{id}, #{studentCourseId}, #{status}
    )
  </insert>

  <!-- 申込状況を更新する -->
  <update id="updateCourseApplicationStatus">
    UPDATE course_application_status
    SET
    student_course_id = #{studentCourseId},
    status = #{status}
    WHERE id = #{id}
  </update>



  <!-- コースマスタの全件検索　-->
  <select id="searchCourseMasterList" resultType="raisetech.StudentManagement.data.CourseMaster">
    SELECT *
    FROM course_master
  </select>

  <!-- コースマスタの検索　-->
  <select id="searchCourseMasterById" resultType="raisetech.StudentManagement.data.CourseMaster">
    SELECT *
    FROM course_master
    WHERE id = #{id}
  </select>


</mapper>